---
- name: Install web packages
  ansible.builtin.dnf:
    name: "{{ managed_httpd_packages }}"
    state: present

- name: Ensure firewalld is running
  ansible.builtin.systemd_service:
    name: firewalld
    enabled: true
    state: started

- name: Ensure the mock docroot exists
  ansible.builtin.file:
    path: "{{ managed_httpd_docroot }}"
    state: directory
    owner: root
    group: root
    mode: "0755"

- name: Deploy the web role configuration
  ansible.builtin.template:
    src: site.conf.j2
    dest: /etc/httpd/conf.d/mock-site.conf
    owner: root
    group: root
    mode: "0644"
  notify: restart httpd

- name: Deploy the index page
  ansible.builtin.template:
    src: index.html.j2
    dest: "{{ managed_httpd_docroot }}/index.html"
    owner: root
    group: root
    mode: "0644"

- name: Validate the full httpd configuration
  ansible.builtin.command: /usr/sbin/httpd -t
  register: mock_httpd_validation
  changed_when: false
  failed_when: mock_httpd_validation.rc != 0

- name: Ensure httpd is enabled and started
  ansible.builtin.systemd_service:
    name: "{{ managed_httpd_service_name }}"
    enabled: true
    state: started

- name: Open the HTTP service in the firewall
  ansible.posix.firewalld:
    service: "{{ managed_httpd_firewall_service }}"
    permanent: true
    immediate: true
    state: enabled

- name: Show a short role deployment summary
  ansible.builtin.debug:
    msg:
      - "Validated httpd configuration on {{ inventory_hostname }}"
      - "Document root: {{ managed_httpd_docroot }}"
      - "Firewall service opened: {{ managed_httpd_firewall_service }}"
